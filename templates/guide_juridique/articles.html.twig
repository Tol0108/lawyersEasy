{% extends 'base.html.twig' %}

{% block content %}
    <h1>Liste des articles</h1>
    <ul>
        {% for article in articles %}
            <li>
                <a href="{{ path('articles', {'id': article.id}) }}">{{ article.titre }}</a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
